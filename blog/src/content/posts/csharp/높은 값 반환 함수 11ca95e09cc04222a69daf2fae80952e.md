# 높은 값 반환 함수

생성일: 2023년 4월 20일 오후 1:18
속성: 알고리즘

```csharp
static int GetHighestScore(int[] scores)
{
		int maxValue = 0;
		foreach (int score in scores)
		{
				if(score >= maxValue)
				{
						if (score > maxValue)
						    maxValue = score;
				}
		}
		return maxValue;
}
```